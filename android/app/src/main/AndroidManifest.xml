<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Billing (Google Play In-App Purchases) -->
    <uses-permission android:name="com.android.vending.BILLING"/>
    <!-- Android 13+: declara que o app NAO usa ID de publicidade -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove"/>

    <application
        android:label="Zoeira Car"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:hardwareAccelerated="true">

        <!-- Impede o FirebaseInitProvider de criar o app [DEFAULT] nativo
             antes do Dart; o Firebase.initializeApp(options:) do main.dart
             é a única inicialização (evita erro "duplicate-app") -->
        <meta-data
            android:name="firebase_automatic_initialization"
            android:value="false"/>

        <!-- Firebase REMOVEU o suporte a esse meta-data; remove o provider
             por completo (também é a forma documentada de desativar o
             auto-init) para o [DEFAULT] ser criado só pelo Dart -->
        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            android:authorities="${applicationId}.firebaseinitprovider"
            tools:node="remove" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Splash screen -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Não exibir a activity em recents quando o app for minimizado -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <!-- Declara que o app usa feature de billing -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false"/>

</manifest>
