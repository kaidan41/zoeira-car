{
  "name": "zoeira-car-functions",
  "description": "Validação de compras (assinatura + consulta avulsa) do Zoeira Car com a API do Google Play",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.3.0",
    "firebase-functions": "^5.0.1",
    "google-auth-library": "^9.11.0",
    "googleapis": "^140.0.0"
  }
}