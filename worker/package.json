{
  "name": "zoeira-car-billing-worker",
  "private": true,
  "description": "Validação de compras (assinatura + consulta avulsa) na API do Google Play, hospedada no Cloudflare Workers (plano free).",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^5.6.3"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}